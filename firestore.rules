rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /users/{userId} {
      // A player can only read and write their own progress data
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
